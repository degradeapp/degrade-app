name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    needs: [ test ]  # Wait for CI to pass

    steps:
      - uses: actions/checkout@v4

      - name: Deploy to production
        env:
          DEPLOY_KEY: ${{ secrets.DEPLOY_KEY }}
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
        run: |
          # Fase 19: Deployment will be implemented here
          echo "Deployment configuration placeholder"
          # This will be implemented in Fase 19 with Coolify integration

      - name: Run smoke tests
        run: |
          # Verify deployment was successful
          echo "Smoke tests placeholder"
